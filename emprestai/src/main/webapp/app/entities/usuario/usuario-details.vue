<template>
  <div class="row justify-content-center">
    <div class="col-8">
      <div v-if="usuario">
        <h2 class="jh-entity-heading" data-cy="usuarioDetailsHeading">
          <span v-text="$t('emprestaiApp.usuario.detail.title')">Usuario</span> {{ usuario.id }}
        </h2>
        <dl class="row jh-entity-details">
          <dt>
            <span v-text="$t('emprestaiApp.usuario.username')">Username</span>
          </dt>
          <dd>
            <span>{{ usuario.username }}</span>
          </dd>
        </dl>
        <button type="submit" v-on:click.prevent="previousState()" class="btn btn-info" data-cy="entityDetailsBackButton">
          <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.back')"> Back</span>
        </button>
      </div>
    </div>
  </div>
</template>

<script lang="ts" src="./usuario-details.component.ts"></script>
